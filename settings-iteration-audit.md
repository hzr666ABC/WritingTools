# 设置页迭代审计

日期：2026-07-19

## 覆盖流程

1. 打开设置页并检查常规、外观、AI 服务三个区块。
2. 从 Gemini 切换到 OpenAI，再切换到 Ollama。
3. 在服务选择区域与模型下拉框上滚动鼠标滚轮。
4. 切换服务后返回，确认未保存的输入仍被保留。
5. 选择自定义背景，确认设置页显示真实图片预览。

## 基线问题

- 服务使用下拉框，悬停滚轮会意外改变当前服务。
- 标题、区块标题、标签与正文之间的字号比例不稳定。
- OpenAI 与 Ollama 的动态设置控件在切换时可能重叠，并被底部保存按钮遮挡。
- 页面提示“立即生效”，但实际需要保存，文案与行为不一致。
- 自定义背景只有文件路径，没有可见预览。

## 本次改进

- 使用三张带真实服务图标的可点击卡片替代服务下拉框；滚轮不再切换服务。
- 重建 24 / 16 / 14 / 13 / 12 px 的文字层级，并统一表单为纵向标签布局。
- 将服务详情固定在稳定容器中，切换时立即隐藏旧控件，消除重叠和残影。
- 模型下拉框只有在用户明确点击并获得焦点后才响应滚轮。
- 切换服务时临时保留尚未保存的输入，减少误操作损失。
- 增加自定义背景真实预览、快捷键格式校验和预设快捷键冲突提示。
- 修正文案为“保存后立即生效，无需重新启动”。

## 验证结果

- 服务滚轮防误触：通过。
- 点击卡片切换：通过。
- 模型下拉框滚轮防误触：通过。
- 服务草稿保留：通过。
- 自定义背景预览：通过。
- 小窗口滚动与固定保存按钮：通过。
- 键盘焦点：服务卡片保留可见焦点边框，并设置可访问名称。

视觉证据保存在任务产物目录的 `settings-iteration-audit` 文件夹中，其中 `settings-openai-before-after.png` 是同流程前后对比图。
