# 单实例与高级简约界面审计

日期：2026-07-19

## 流程与健康度

1. 启动应用：基线存在重复启动后新增整套后台进程的风险；修复后由 Windows 命名互斥体保证单实例，健康度：通过。
2. 再次点击桌面快捷方式：不会初始化第二套应用，而是向现有实例发送激活信号并打开已有界面，健康度：通过。
3. 打开快捷键弹窗：预设列表、数字快捷键、自定义指令与编辑入口完整，健康度：通过。
4. 使用方向键切换预设：左侧靛蓝细轨、文字权重和数字徽标同步更新，健康度：通过。
5. 打开设置页：常规、外观、AI 服务和固定保存操作区无重叠，健康度：通过。

## 主要发现与处理

- 重复启动此前没有进程级互斥；新增 Windows 命名互斥体和激活事件。
- 源码集成验证中，首次运行包含解释器启动链的 2 个进程；再次启动后仍为 2 个，第二次启动以状态码 0 退出。
- 成品 EXE 验证中，首次运行包含 PyInstaller 启动链的 2 个进程；连续再启动 3 次后仍为 2 个，三次重复启动均以状态码 0 退出。
- 设置窗口此前每次调用都新建对象；现在同一模式下复用并置前已有窗口。
- 弹窗选中态采用大块卡片，视觉较重；现在改为连续列表、细分隔线和左侧聚焦轨。
- 记忆模式位于主内容顶部，干扰快速选择；现在移到列表下方的低干扰状态区。
- 设置页主按钮横跨全宽，视觉权重过高；现在使用右对齐紧凑主按钮与本地保存提示。

## 可访问性与限制

- 方向键、Enter、数字键、Esc 与 Tab 焦点路径均保留。
- 记忆开关仍为可点击、可聚焦的真实控件，并保留可访问名称。
- 截图可以验证层级、对比度和可见焦点，但不能单独证明屏幕阅读器的完整朗读顺序。

完整截图与对照位于任务产物目录的 `premium-ui-audit` 文件夹。
