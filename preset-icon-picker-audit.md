# 预设编辑与图标库审计

日期：2026-07-20

## 流程与健康度

1. 打开预设编辑模式：编辑与删除操作集中在右侧，不再遮挡预设图标，健康度：通过。
2. 拖动预设排序：除右侧操作区外的整行仍可作为拖动目标，健康度：通过。
3. 新增或编辑预设：显示 12 个真实图标及中文名称，健康度：通过。
4. 点击图标：选中项出现靛蓝描边和浅色背景，健康度：通过。
5. 保存预设：所选图标 ID 写入 `options.json` 并立即用于弹窗列表，健康度：通过。

## 验证

- 编辑操作区与左侧内容图标区几何不相交。
- 图标库包含 12 个唯一 ID，每个 ID 都有浅色和深色 PNG 文件。
- 编辑已有预设时会还原当前图标；无效的旧图标值安全回退到“灵感”。
- 图标按钮具有工具提示、可访问名称、鼠标与键盘焦点能力。

完整截图保存在任务产物目录的 `preset-icon-picker-audit` 文件夹。
