# 快捷键录制功能验证

日期：2026-07-20

## 覆盖范围

1. 设置页主快捷键：点击输入框并按下组合键后立即显示标准化结果。
2. 自定义预设独立快捷键：编辑预设时使用同一录制控件。
3. 首次引导主快捷键：新用户无需理解快捷键文本格式。
4. 清除与取消：Backspace/Delete 清空，Esc 保留原值并退出录制焦点。

## 验证结果

- `Ctrl+Shift+P` 识别为 `ctrl+shift+p`。
- `Ctrl+Space` 识别为 `ctrl+space`。
- `Ctrl+Alt+W`、`Ctrl+Shift+J` 与单键 `F8` 均成功识别。
- 录制结果继续经过原有格式校验与快捷键冲突检测。
- 输入框为只读录制控件，不能再因手工拼写造成格式错误。

离屏界面证据保存在任务产物目录的 `shortcut-recorder-audit` 文件夹。
